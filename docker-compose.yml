services:
  positive:
    build: .
    container_name: positive_engine
    ports:
      - "${PORT:-8178}:8000"
    volumes:
      - ./data:/app/data
      - ./media:/app/media
      - ./templates:/app/templates
    env_file:
      - .env
    restart: unless-stopped
